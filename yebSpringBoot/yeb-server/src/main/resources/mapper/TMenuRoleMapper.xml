<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.icatw.yeb.server.mapper.TMenuRoleMapper">

    <resultMap type="cn.icatw.yeb.server.domain.TMenuRole" id="TMenuRoleMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="mid" column="mid" jdbcType="INTEGER"/>
        <result property="rid" column="rid" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询指定行数据-->
    <select id="selectTMenuRoleList" resultMap="TMenuRoleMap">
        select
        id, mid, rid
        from t_menu_role
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="mid != null">
                and mid = #{mid}
            </if>
            <if test="rid != null">
                and rid = #{rid}
            </if>
        </where>
    </select>
</mapper>
