<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.icatw.yeb.server.mapper.TDepartmentMapper">

    <resultMap type="cn.icatw.yeb.server.domain.TDepartment" id="TDepartmentMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="parentid" column="parentId" jdbcType="INTEGER"/>
        <result property="deppath" column="depPath" jdbcType="VARCHAR"/>
        <result property="enabled" column="enabled" jdbcType="INTEGER"/>
        <result property="isparent" column="isParent" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询指定行数据-->
    <select id="selectTDepartmentList" resultMap="TDepartmentMap">
        select
        id, name, parentId, depPath, enabled, isParent
        from t_department
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="parentid != null">
                and parentId = #{parentid}
            </if>
            <if test="deppath != null and deppath != ''">
                and depPath = #{deppath}
            </if>
            <if test="enabled != null">
                and enabled = #{enabled}
            </if>
            <if test="isparent != null">
                and isParent = #{isparent}
            </if>
        </where>
    </select>
</mapper>
